{
  "name":        "pixiv",
  "description": "pixiv.com downloader",
  "version":     "0.0.0",
  "origin":      "/(www\\.)?pixiv\\.net/",
  "flags":       [
    ["-u", "--username", "string", "username to login with"],
    ["-p", "--password", "string", "password to login with"]
  ],
  "entry": "pixiv.ls",
  "deps":  {
    "request": "^2.55.0"
  }
}
